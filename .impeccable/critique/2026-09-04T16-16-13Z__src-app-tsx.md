---
target: src/App.tsx
total_score: 25
max_score: 40
na_heuristics: 
p0_count: 1
p1_count: 2
target_identity: "file:/Users/hannahscaife/Documents/GitHub/post-mortem-frontend/src/App.tsx"
target_fingerprint: "sha256:16d6cfd63cab2621802c47b3e59ee072f2e8a186a033cd58540922899c90c44e"
target_path: /Users/hannahscaife/Documents/GitHub/post-mortem-frontend/src/App.tsx
timestamp: 2026-09-04T16-16-13Z
slug: src-app-tsx
---
**Method:** dual-agent (Assessment A: design review · Assessment B: detector + browser evidence), run as two isolated sub-agents with no visibility into each other's output.

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Live clock/countdowns strong; no session/connection status anywhere |
| 2 | Match System / Real World | 4 | Wire-service vocabulary coherently mapped throughout |
| 3 | User Control and Freedom | 2 | No un-claim, no drill-in/back, no filters |
| 4 | Consistency and Standards | 3 | Internally disciplined; deliberately departs from dashboard convention |
| 5 | Error Prevention | 2 | No confirmation needed for claim (correct), but zero concurrent-claim guard |
| 6 | Recognition Rather Than Recall | 4 | Live countdowns remove all mental tracking |
| 7 | Flexibility and Efficiency | 1 | No filters, sort override, "mine" view, or shortcuts |
| 8 | Aesthetic and Minimalist Design | 4 | Disciplined, no decoration for its own sake |
| 9 | Error Recovery | 1 | No error state exists anywhere in the codebase |
| 10 | Help and Documentation | 1 | No legend mapping FLASH/URGENT/BULLETIN/ROUTINE to severity |
| **Total** | | **25/40** | **Acceptable** |

## Design Specificity Verdict

LLM assessment: PASS, genuinely domain-grounded. Wire-service metaphor carried throughout: precedence vocabulary, claim-as-stamp, RECEIVING WIRE loading state, -30- sign-off, day/night desk as two light sources not an inversion.

Deterministic scan: detect.mjs ran DEGRADED (missing htmlparser2/css-select/css-tree/domutils, regex fallback). CLI reported 0 findings (undercount). Browser overlay (DOM/CSSOM-aware) caught 1 real finding the CLI missed: blinking-cursor anti-pattern on LoadingState.tsx's infinite steps(1) blink block. Mitigated with aria-hidden + sr-only text, but the rule fired correctly.

Visual overlays: ran in an isolated sub-agent's tab, since closed.

## Overall Impression

Wire-desk world is real and well-executed at the surface level. Both assessments converge: the frontend hasn't caught up to what PRODUCT.md says makes this product exist. Read-time TTL truth is modeled honestly; optimistic locking has zero representation.

## What's Working

1. Read-time truth honestly modeled: mitigationClock() recomputes against a live clock every second.
2. Color discipline holds: severity stays typographic-only, color reserved for live/urgent status and TTL alarms.
3. Theme execution: light "day desk" palette is a fully considered alternate reading, not a lazy inversion.

## Priority Issues

[P0] Optimistic locking has no frontend representation at all
- Why it matters: PRODUCT.md names this as one of three defining mechanisms. Incident has no version field; handleClaim does a blind local overwrite.
- Fix: Add version to the type; design a placeholder "STALE - reload required" bulletin state.
- Suggested command: /impeccable harden

[P1] No incident detail/description surface
- Why it matters: PRODUCT.md's Incident entity has a description; UI never renders past a one-line title.
- Fix: Add the field, at minimum a collapsed/expandable secondary line.
- Suggested command: /impeccable shape

[P1] No session identity or role model
- Why it matters: ClaimControl hardcodes "H. SCAIFE" as whoever clicks. admin/responder/viewer roles are backend-enforced and load-bearing.
- Fix: Surface current user/role in the header; gate ClaimControl for viewers.
- Suggested command: /impeccable harden

[P2] Loading state still reflows, and trips a real motion anti-pattern
- Why it matters: finish-reviewer's verdict pass scored this "partial"; Assessment B's detector independently flagged the same component's infinite blink cursor.
- Fix: Sync skeleton count to real incident count, add placeholder bars for mitigation/claim rows, reconsider the blinking cursor.
- Suggested command: /impeccable optimize

[P2] No error/failure state
- Why it matters: LoadingState is the only pre-content state that exists. A real fetch failure would hang forever with no explanation.
- Fix: Add an explicit error state alongside loading/empty.
- Suggested command: /impeccable harden

## Persona Red Flags

Riley (stress-tester): concurrent claims produce silent last-write-wins with no conflict surfaced.

Alex (impatient power user): no filters, no "mine" view, no shortcuts, no bulk actions. Heuristic 7 scores 1/4.

Sam (accessibility-dependent): focus states strong, color isn't sole severity signal, touch targets clear 44px. Gap: PrecedenceStamp text has no aria-label mapping severity.

## Minor Observations

- "-30-" risks reading as "30 incidents" when only 6 render.
- mttr_seconds, PRODUCT.md's named headline metric, is never shown; UI displays time-since-resolution instead.
- Light-theme status text (#b93318 on #f2ead7) measures ~4.9:1, clears the floor with little margin.

## Questions to Consider

- If optimistic-locking conflicts are one of three defining mechanisms, why does the shipped UI have no visual language for that state?
- Is "one flat triage list, nothing else" the intended scope forever, or is a detail/audit view coming?
